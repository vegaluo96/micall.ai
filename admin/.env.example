# MiCall 运营管理端配置（CLAUDE.md 铁律2：endpoint/key 走配置，不硬编码）
#
# 后端管理 API 基址（REST）。配置后：
#   - 登录走后端：POST {VITE_API_BASE}/admin/login（真鉴权、发 token）
#   - 列表/详情等数据走后端
#   - 「接口配置」的 endpoint/key 读写、连通性测试走 /admin/api-config[/test]
#     => 密钥存服务端、读取由后端打码，浏览器不持久化明文密钥（生产务必配置）
VITE_API_BASE=

# 无后端时，后台登录页的「软门禁」密码（仅 UX，真防护靠 nginx Basic Auth）。
# 配置了 VITE_API_BASE 后此项忽略（改由后端校验）。默认 luoweijia。
VITE_ADMIN_PASSWORD=
