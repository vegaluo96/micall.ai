# MiCall 运营管理端配置（CLAUDE.md 铁律2：endpoint/key 走配置，不硬编码）
#
# 后端管理 API 基址（REST）。配置后：
#   - 列表/详情等数据走后端
#   - 「接口配置」的 endpoint/key 读写走 GET/PUT {VITE_API_BASE}/admin/api-config，
#     连通性测试走 POST {VITE_API_BASE}/admin/api-config/test
#     => 密钥存服务端、读取时由后端打码，浏览器不持久化明文密钥（生产务必配置）
#
# 留空 = 使用内置 mock 数据；「接口配置」落 localStorage，仅供本地联调。
# 例如：VITE_API_BASE=https://api.micall.ai
VITE_API_BASE=
