# MiCall 前端配置（CLAUDE.md 铁律2：所有外部服务 endpoint 走配置，不硬编码）
#
# 实时控制信令端点（WebSocket）。留空 = 使用内置 MockSignalingClient，
# 应用可在无后端的情况下独立运行（用于复刻验收 / UI 联调）。
# 接入真实后端时填入，例如：
# VITE_SIGNALING_URL=wss://api.micall.ai/realtime/signal
VITE_SIGNALING_URL=
